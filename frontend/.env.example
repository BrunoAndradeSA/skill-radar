# Skill Radar — Environment Variables
# Copy this file to .env and adjust as needed.

# Mock mode: true = dados locais (sem backend), false = API real
VITE_MOCK_MODE=true

# URL base da API REST (obrigatório quando VITE_MOCK_MODE=false)
VITE_API_URL=http://localhost:8000/api/v1
